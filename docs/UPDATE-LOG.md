# Журнал обновлений — Планово

## Обновление от 16 июля 2026 (education-фокус, TASK-30)

- **Полный разворот на учебные заведения**: все тексты лендинга переписаны под колледжи/школы/вузы; убраны все упоминания спортивных секций и клубов
- «Знакомо?» — три ситуации теперь про учебную часть, преподавателя и студентов; форма: «Тип учреждения» вместо «Ниша»
- Секция «Ниши» → секция «Демо» с одной картой «Конструктор расписания учебной части»
- **Демо: конструктор — главный экран.** Роль «Учебная часть» по умолчанию; полный `AdminView` с сеткой 5 дней × 4 пары (~70 занятий): перенос и удаление занятий кликами, добавление с выбором дня/пары, публикация (блокируется при конфликтах), автогенерация (room-aware — не бронирует занятую аудиторию), вкладка конфликтов
- **Детальные конфликты**: во всех типах указаны день, пара со временем, чётность недели, дисциплина, группа, аудитория и участники
- **Автогенерация**: честная красная плашка со списком конфликтов (была зелёная «успешно» даже при конфликтах)
- Guided-сценарий «Преподаватель заболел» — второстепенный этап (id `#scenario`, старт по кнопке); удалены `sports.html/clubs.html/*.demo.js` и все ссылки

## Обновление от 10 июля 2026 (посекционная подача лендинга, TASK-29)

- **Один экран — одна секция**: hero занимает ровно вьюпорт (следующая секция больше не выглядывает снизу), каждая смысловая секция — `min-height: 100svh` с вертикальным центрированием контента
- Исправлена причина: правило `.landing-preview section { min-height: 1px }` (наследие runtime-патчинга TASK-25) глушило `min-height: 100vh` у hero
- На десктопе (≥769px) — мягкая привязка прокрутки `scroll-snap-type: y proximity`: короткие секции выравниваются по экрану, длинные (ниши, процесс) скроллятся свободно; на мобильном snap отключён
- Проверено: якоря навигации садятся в начало секций, FAQ раскрывается, низкие окна не режут hero (растёт под контент), мобильный без горизонтального скролла

## Обновление от 9 июля 2026 (guided-демо v2: интерактивные сценарии, TASK-28)

- **Каждое демо теперь начинается с интерактивного guided-сценария** (референс-паттерн: Navattic/Arcade-style guided walkthrough): посетитель сам выполняет ключевое действие продукта за 2–4 клика с коуч-марками и прогрессом шагов; витрина по ролям осталась ниже
- **education** — «Преподаватель заболел»: перенос занятия → живой конфликт из `getConflicts` (преподаватель + аудитория, в `getConflicts` дописаны room-конфликты по образцу Next-стора) → разрешение → публикация → payoff: телефон ученика с бейджем «Обновлено только что»
- **sports** — «Тренер заболел»: отмена тренировки с подтверждением → payoff: телефон клиента с отменой + «Группа уведомлена (N участников)»
- **clubs** — «Запишитесь на турнир»: RSVP с лимитом мест (18/20 → 19/20) → payoff: вид организатора с баром заполненности 95%
- Движок шагов (`useGuidedScenario`/`GuidedFrame`/коуч-марк) продублирован в каждый `*.demo.js` перед `// MAIN APP` (паттерн репо) — при правках синхронизировать вручную
- **Удалены `scripts/rebuild-demos.js` и `scripts/compile-demos.js`** — они восстанавливали `*.demo.js` из старых git-коммитов и стёрли бы guided-код; демо-файлы правятся только руками
- Режимы: «Пропустить» → свободная песочница стейджа (education: перенос любых ячеек с живыми конфликтами); финал → CTA «Получить бесплатный разбор» → `index.html#contact`; повторный визит → replay-баннер (localStorage `planovo.guided.*`)
- Новый `assets/demo-guided.css` (общий для 3 страниц, тема ниши через `var(--primary)`); мобильный ≤767px: коуч-марк = fixed bottom-sheet, тач-цели ≥44px; `prefers-reduced-motion` и `@media print` учтены

## Обновление от 9 июля 2026 (лендинг v2: структура по akeda.ru, статика без React, TASK-26)

- **Исправлено «мигание» старого интерфейса**: React/Next.js полностью убран из `index.html` (JS-чанки, RSC-payload, sonner) — страница сразу отдаётся в финальном виде, runtime-патчинг отменён
- Новый `assets/landing-core.js` — бургер-меню + reveal-анимации (замена React 1:1) + страховка для карусели ниш
- Структура по логике akeda.ru: hero с оффером «Бесплатный разбор — 20–30 минут» → «Знакомо?» (3 ситуации с цитатами по нишам) → решение → «Было/Стало» → ниши-демо → «Простота» → возможности → процесс запуска со сроками → **FAQ (новая секция)** → форма «бесплатный разбор»
- Секции `#ease`, `#daily-use` теперь статические в HTML (раньше вставлялись JS); жёлтая палитра запечена в inline-стили; SEO-мета обновлена под новые тексты
- FAQ на `<details>/<summary>` без JS; стили FAQ/цитат добавлены в `landing-review-preview.css`; версии `?v=26` у изменённых ассетов против кэша
- Тексты формы: «Заявка на бесплатный разбор», кнопка «Получить бесплатный разбор» (также в шаблоне `site-legal.js`)
- Удалены: `scripts/patch-index-legal.js`, `scripts/build-landing-preview.mjs`, `index-preview.html`, мёртвый билд `_next/static/hV-XyMrDia8ttmsTZDmtZ/`, чанки-сироты
- Оставлены на переходный период (stale-кэш): `assets/landing-review-preview.js`, `_next/static/chunks/4c37fd759bda90ef.js` — удалить в TASK-27

## Обновление от 18 июня 2026 (лендинг по рецензии, TASK-25)

- Жёлтая палитра вместо фиолета, новый логотип (градиент календарь), обновлённый копирайт
- Секции «Было/Стало», «Каждый день», упрощённое «Решение» (2 карточки + light mockup)
- Hero: «Расписание без Excel и нервов», badge «Без Excel и PDF»
- `assets/landing-review-preview.css`, `assets/landing-review-preview.js` — патч после React-hydrate
- `scripts/patch-index-legal.js` подключает review-ассеты на прод; `index-preview.html` — локальный QA с лентой PREVIEW
- `scripts/build-landing-preview.mjs` — сборка preview из `index.html`

## Обновление от 18 июня 2026 (новый логотип, TASK-23)

- Новый знак: календарь + круг «процесс», градиент фиолетовый → magenta
- Исходник `assets/logo-source.png`; пайплайн `scripts/process-logo.py` → `logo.png`, `assets/logo.png`, `favicon-32.png`
- Прозрачный фон (white→alpha), trim, 1024×1024

## Обновление от 18 июня 2026 (откат визуала линии «Процесс» к PR #20)

- Убран левый rельс и `z-index: 3` у SVG (линия больше не поверх карточек)
- Якоря path снова по центру badge; прогресс скролла `0.84` / `0.68` как в PR #20
- Self-boot init и карусель ниш **без изменений**

## Обновление от 18 июня 2026 (fix: карусель ниш + анимация процесса)

- **Карусель:** `translate3d(-100%)` сдвигала на 3 карточки сразу — переведено на px (`viewport.clientWidth`)
- **Процесс:** убран `getScrollProgressMobile` (мгновенный progress=1), снова прогресс по badge при скролле

## Обновление от 18 июня 2026 (fix: «Процесс» не инициализировался на mobile)

- **Причина:** `PlanovoProcessScroll.init()` ждал IntersectionObserver и часто **не вызывался**; SVG не создавался вообще
- **Fix:** self-boot в `process-scroll.js` (DOMContentLoaded + load); повторные попытки в `site-legal.js` (400/1200/2500 ms, mutation observer)
- Линия в левом рельсе (`x=28px`), серая дорожка `.process-path-track` всегда видна

## Обновление от 18 июня 2026 (fix: линия «Процесс» на мобильном)

- Линия SVG была **под** белыми карточками (z-index 0) — на вертикальном стеке её не было видно
- Рельс слева: badge в gutter, path `z-index: 3`, прогресс скролла для mobile

## Обновление от 18 июня 2026 (ниши — мобильная карусель, TASK-19)

- Секция **«Ниши»** на телефоне: одна карточка + кнопка-стрелка справа (цикл 1→2→3)
- Кнопки **«Попробовать демо»** выровнены по нижнему краю карточки (`flex` + `margin-top: auto`)
- Десктоп без изменений; `initNicheCarousel()` в `site-legal.js`

## Обновление от 18 июня 2026 (mobile UI fixes — TASK-18)

- **Проблема:** `overflow: visible` на секции/гриде; z-index для hot-карточки; аура первой/последней строки без обрезки (`problem-aura.css`, `site-mobile-landing.css`, `site-mobile.css`)
- **Демо (mockup):** все 5 вкладок — горизонтальный скролл с snap на ≤768px (`landing-mockup.css`, fallback в `site-mobile.css`)
- **Процесс:** вертикальный стек шагов вместо absolute %; SVG-линия по badge при скролле (`process-scroll.js`, `process-scroll.css`)

## Обновление от 18 июня 2026 (мобильный лендинг v3 — compact)

- Восстановлен **`.mobile-layout`**: `site-mobile-landing.css` + `syncMobileLandingLayout()` в `site-legal.js`
- Компактная типографика (`clamp`), секции **20–32px**, hero без `100vh`
- «Проблема» — список-строки в одной карточке; ниши — snap-карусель **72vw**
- Fallback-уплотнение в `site-mobile.css`

## Обновление от 18 июня 2026 (education: showcase преподавателя)

- Вкладка **Преподаватель**: второй блок — пункт **2** («Группы в карточках») вместо «Доступность»; прозрачный accent на пункте 2

## Обновление от 18 июня 2026 (ниши + accent демо)

- Лендинг: кнопки **«Попробовать демо»** в карточках ниш; секция `#demos` скрыта; якоря → `#niches`
- `assets/site-legal.js`, `scripts/patch-index-legal.js`, `assets/demo-nav-fallback.js`
- Демо (education / sports / clubs): прозрачный accent только у **пункта 2**; пункты 1 и 3 — белый фон

## Обновление от 18 июня 2026 (откат mobile-layout + фиксы)

- Отключён `site-mobile-landing.css` — лендинг на телефоне снова как в базовом бандле (до TASK-11)
- Hero: скрыт индикатор прокрутки вниз (`.hero-scroll`)
- Процесс: убран `process-stack`, SVG-линия снова рисуется при скролле на всех ширинах
- Демо (education / sports / clubs): пункты **1** и **3** выделены акцентным фоном и цветом пояснений

## Обновление от 18 июня 2026 (mobile fixes)

- Hero: вертикальный стек без наложений, логотип 36px в шапке на телефоне
- Проблема: крупные карточки (иконка + заголовок + текст)
- Демо: вертикально «Шаг 1/2/3» с отступами
- Процесс: вернута SVG-линия на мобильном, разделители между шагами
- Решение: mockup не обрезается, табы и кнопки интерактивны

## Обновление от 18 июня 2026 (мобильный лендинг v2 — skills)

### Пересборка по GitHub skills

Скачаны skills в `.cursor/skills/`: `responsive-design`, `mobile-design`, `responsive-testing`, `web-design-guidelines` (см. README).

- `site-mobile-landing.css` — **v2**: 8px-сетка, `clamp()` типографика, touch ≥44px
- **Проблема** — list-in-card (строки с разделителями)
- **Ниши / демо** — горизонтальные snap-карусели вместо столбика
- **Hero** — выравнивание влево, safe-area, полноширинные CTA
- Десктоп **не изменён** — только `media="(max-width: 768px)"`

## Обновление от 18 июня 2026 (мобильный лендинг)

### Отдельный дизайн для телефона (≤768px)

- `assets/site-mobile-landing.css` — подключается **только на мобильном** (`media="(max-width: 768px)"`), не масштабирует десктоп
- Класс `.mobile-layout` на `.landing-page` — компактные секции, мелкая типографика
- **Проблема** — строки (иконка + текст), не большие плитки
- **Возможности** — сетка 2×N мини-карточек
- **Процесс** — вертикальный стек (`process-stack`), без SVG-таймлайна и min-height 1200px
- Mockup в «Решении» — превью до 210px, сайдбар скрыт
- `assets/site-legal.js` — `syncMobileLandingLayout()`, инъекция `site-mobile-landing.css`
- `scripts/patch-index-legal.js` — link с `media="(max-width: 768px)"` в `index.html`
- Документация: `docs/RESPONSIVE.md` §4.0

## Обновление от 18 июня 2026 (бренд и лендинг)

### Логотип Планово

- `logo.png` / `assets/logo.png` — оригинальная иконка (календарь + лавандовый круг), белый фон убран, 1024×1024
- `scripts/process-logo.py` — обработка исходника (trim, sharpen, upscale)
- Логотип **60×60** в шапке лендинга, legal-страниц и демо; favicon → `logo.png`
- `assets/site-legal.js` — подмена emoji/React после гидратации (`upgradeLandingLogo`)

### Лендинг: фон, «Проблема», mockup

- `assets/landing-background.css` — единый hero-градиент и точечная сетка по странице
- `assets/problem-aura.css` + `problem-aura.js` — красное свечение карточек секции «Проблема» при скролле
- `assets/landing-mockup.css` — фикс скачка размера панели между вкладками mockup
- `assets/site-legal.js` — скрытие блока hero-stats (3+/2/1), инъекция новых ассетов
- `assets/process-scroll.js` — уточнён прогресс линии у последнего шага

## Обновление от 18 июня 2026

### Секция «Процесс» — scroll-таймлайн (пересборка)

- `assets/process-scroll.js` + `assets/process-scroll.css` — пересобрано по паттерну **scroll-storyteller** (draw-path + scrub)
- Органическая SVG-кривая (Catmull-Rom) через центры badge шагов; glow-слой и узлы на каждом шаге
- Прогресс линии привязан к прохождению блока через viewport (аналог ScrollTrigger `scrub`), плавная интерполяция точки
- Карточки шагов: reveal при скролле, активный шаг с пульсом кольца, пройденные — галочка
- Фон секции: мягкий градиент и рамка canvas

### Кнопки «Попробовать демо» на лендинге

- В `index.html` кнопки `.demo-card-btn` заменены на `<a href="education.html|sports.html|clubs.html">` — работают без JS
- `scripts/patch-index-legal.js` — тот же патч при пересборке index
- `assets/demo-nav-fallback.js` — запасной перехват кликов до React `setView`
- `assets/site-legal.js` — `data-demo-key`, ссылки в футере «Ниши» → standalone-демо

## Обновление от 16 июня 2026

### Мобильная адаптация сайта

- `assets/site-mobile.css` — лендинг, форма заявки, legal-страницы, cookie-баннер
- `assets/demo-mobile.css` — демо education / sports / clubs: шапка, showcase, сетки, табы
- Убраны жёсткие `min-width` у расписаний; поля формы 16px на iOS
- Fix: секция «Решение» (mockup) не выходит за край экрана на телефоне
- **`docs/RESPONSIVE.md`** — спецификация брейкпоинтов и правил по устройствам

### Демо: визуальный showcase (education, sports, clubs)

- Модель **«визуальный пример + пояснения»**: блочный тур (комментарий слева, UI справа)
- Упрощённые вкладки ролей; сетка **Пн + Ср × 2 слота** вместо полной недели
- Doodle-фон страницы (Telegram-style SVG) для каждой ниши
- `education.html` / `sports.html` / `clubs.html` + соответствующие `*.demo.js`
- План и статус: `docs/Demoplan.md`

## Обновление от 16 июня 2025

### Что нового

#### Главная страница (Landing)
- Полноценный лендинг с hero-секцией "Планово — Расписание без хаоса"
- Секция "Возможности платформы" с 4 карточками
- Секция **"Наше решение"** с тремя интерактивными карточками-демо
- Кнопки "Открыть демо" для каждой ниши
- Адаптивная мобильная версия

#### Демо: Учебные учреждения
- 3 роли: Ученик, Преподаватель, Учебная часть
- Расписание с чёт/нечёт неделями
- Конфликт-детектор
- Автогенерация расписания
- Доступность преподавателей
- Совмещённые пары

#### Демо: Спортивные секции (НОВОЕ)
- 3 роли: Спортсмен, Тренер, Администратор
- 9 видов спорта, 11 тренеров, 10 площадок
- Расписание тренировок
- Посещаемость и аналитика
- Автогенерация расписания
- Зелёный акцент (#10b981)

#### Демо: Клубы и мероприятия (НОВОЕ)
- 3 роли: Участник, Организатор, Администратор
- 9 мероприятий, 11 организаторов, 10 помещений
- Календарь событий
- Бронирование ресурсов
- Статистика вовлечённости
- Оранжевый акцент (#f59e0b)

### Технологии
- Next.js 16 + App Router + TypeScript 5
- Tailwind CSS 4 + shadcn/ui
- Zustand для управления состоянием
- Статический экспорт для GitHub Pages

### Структура навигации
- Главная → 3 карточки-демо → Кнопка "Открыть демо"
- В каждом демо: кнопка "назад" (←) для возврата на главную
- Внутри демо: переключение ролей через кнопки в хедере

---

## 2026-06-18 — Реквизиты ИП в юридических документах

- **privacy.html** — оператор ПДн (ИП Шпарага А.Д., ИНН, ОГРНИП, адрес) + раздел «Реквизиты» с банковскими данными
- **consent-pdn.html** — оператор в тексте согласия
- **cookies.html** — блок «Контакты оператора»
- **site-legal.js** — реквизиты в футере лендинга
- **site-legal.css** — стили `.legal-requisites`, `.footer-requisites`

---

## 2026-06-18 — Ссылка Telegram @planovoo

- Кнопки «Написать в Telegram» и ссылка Telegram в футере → https://t.me/planovoo
- `site-legal.js`: `TELEGRAM_URL`, `patchTelegramLinks()` для всех ссылок t.me
